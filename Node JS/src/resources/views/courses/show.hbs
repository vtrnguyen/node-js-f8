<div class="mt-4">
  <div class="row">
    <div class="col-lg-3">
      <button class="btn btn-outline-success">HỌC NGAY</button>
      <ul style="margin-top: 15px;">
        <li><p>Độ khó: {{course.level}}</p></li>
        <li><p>Giá khóa học: {{course.price}} coins</p></li>
      </ul>
    </div>

    <div class="col-lg-3">
      <h2>{{course.name}}</h2>
      <iframe
        width="560"
        height="315"
        src="https://www.youtube.com/embed/{{course.videoId}}"
        title="YouTube video player"
        frameborder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
        allowfullscreen
      ></iframe>
      <p>{{course.description}}</p>
    </div>
  </div>
</div>